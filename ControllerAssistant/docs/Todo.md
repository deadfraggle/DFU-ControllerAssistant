# Things to do

## Before Release

- replace CacheField() with the hierarchy-walking version *
- replace CacheMethod() with the hierarchy-walking version *
anywhere that:
menuWindow.GetType()
reflection against fields or methods declared on a DFU base window
support for mods that may subclass or wrap vanilla windows

## Menus to consider for assists

- looting (does it engage the regular Inventory assists?)


## Non priority features

- controller profiler for DirectInput and Switch controllers